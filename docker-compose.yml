services:
  web:
    image: nginx:1.27-alpine
    container_name: dental-mvp-prototype
    ports:
      - "8080:80"
    volumes:
      - ./app:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
